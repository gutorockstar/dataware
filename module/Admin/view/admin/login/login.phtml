<?php

$form = $this->form;
$form->prepare();
$form->setAttribute('action', $this->url('login'));

$formLogin = "{$this->form()->openTag($form)}
              {$this->formCollection($form)}
              {$this->form()->closeTag()}";
              
echo $this->ViewHelper('Efetuar login', array($this->PanelHelper($formLogin)), array('width' => '50%'));

?>