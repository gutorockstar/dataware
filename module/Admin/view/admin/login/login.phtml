<?php

$form = $this->form;
$form->prepare();
$form->setAttribute('action', $this->url('login'));

$formLogin = "{$this->form()->openTag($form)}
              {$this->formCollection($form)}
              {$this->form()->closeTag()}";
              
echo $this->View('Efetuar login', array($this->Panel($formLogin)), array('width' => '50%'));

?>