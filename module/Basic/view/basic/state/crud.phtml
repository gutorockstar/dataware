<?php

/**
$form = $this->form;
$form->prepare();
$form->setAttribute('action', $this->url());

$disableOptions = array(
    \Innerbridge\View\Helper\Toolbar::TB_ACTION_DELETE, 
    \Innerbridge\View\Helper\Toolbar::TB_ACTION_EDIT, 
    \Innerbridge\View\Helper\Toolbar::TB_ACTION_PRINT
);

$formContent .= "{$this->form()->openTag($form)}
                 {$this->formCollection($form)}
                 {$this->form()->closeTag()}";

$viewContent = array(
    $this->Toolbar($disableOptions),
    $this->Panel($formContent)
);         
              
echo $this->View('Novo estado', $viewContent);
 * 
 */

?>

