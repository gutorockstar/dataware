<?php

$toolbar = new \System\Model\Toolbar();
$toolbar->setDisableToolbarActions(array(
    System\Model\Toolbar::TB_ACTION_PRINT,
    System\Model\Toolbar::TB_ACTION_CLONE,
    System\Model\Toolbar::TB_ACTION_UNIFY,
    System\Model\Toolbar::TB_ACTION_SAVE,
));

$attachment = new \System\Model\Attachment($id, $entity);
$tabs = array(
    new \System\Model\Tab("company", "Arquivos anexados", $this->ListAttachmentFilesHelper($attachment), true),
    new \System\Model\Tab("mission", "Upload de arquivos", $this->MultiUploaderHelper($attachment))
);
$panelContent .= $this->TabHelper($tabs);
$panel = new \System\Model\Panel($caption, $panelContent, 'fa-folder-open');

$view = new \System\Model\View();
$view->setToolbar($toolbar);
$view->setPanel($panel);

echo $this->ViewHelper($view);

?>